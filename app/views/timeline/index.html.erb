<% @days.each do |day, data| %>
  <div class="row">
    <div class="span12">
      <h3><%= day.strftime("%B %d, %Y") %></h3>
      <%= render partial: 'events/event', collection: data['events'], as: :event %>
      <%= render partial: 'measurements/measurement', collection: data['measurements'], as: :measurement %>
    </div>
  </div>
<% end %>